export const reducer = (state, action) => {
  switch (((state = []), action.type)) {
    case "FETCH_ALL":
      return posts;
    case "CREATE":
      return posts;
    default:
      return posts;
  }
};
